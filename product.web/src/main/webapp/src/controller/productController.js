define(['controller/_productController','delegate/productDelegate'], function() {
    App.Controller.ProductController = App.Controller._ProductController.extend({

    });
    return App.Controller.ProductController;
}); 